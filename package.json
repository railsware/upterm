{
  "name": "black-screen",
  "version": "0.0.1",
  "main": "src/main.js",
  "dependencies": {
    "jquery": "^2.1.3",
    "pty.js": "*",
    "ansi-to-html": "*",
    "lodash": "*",
    "node-ansiparser": "*"
  },
  "scripts": {
    "install": "npm install -g atom-shell && apm install",
    "compile": "tsc src/*.ts --out compiled/terminal.js -m commonjs",
    "start": "npm run compile && atom-shell ."
  },
  "devDependencies": {
    "typescript": "1.5.0-alpha",
    "atom-shell": "*"
  }
}
