/// <reference path="references.ts" />

module BlackScreen {
    export interface Attributes {
        color?: Color;
        weight?: Weight;
        underline?: boolean;
        crossedOut?: boolean;
    }
}
