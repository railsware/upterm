machine:
  node:
    version: 6.2.0
test:
  post:
    - npm run lint
    - npm run integration-test
deployment:
  release:
    branch: feature-automatic-releases
    commands:
      - npm version patch
      - npm run release
general:
  branches:
    ignore:
      - gh-pages
